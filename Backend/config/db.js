// Este archivo podría usarse para una configuración más avanzada de MongoDB
// Actualmente la conexión está en server.js para simplificar
module.exports = {
  MONGODB_URI: process.env.MONGODB_URI,
};
